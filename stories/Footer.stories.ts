import type { Meta, StoryObj } from '@storybook/react';
import Footer from '../kurr-ads-manager/src/components/navbars/Footer'

const meta: Meta<typeof Footer> = {
  component: Footer,
};

export default meta;
type Story = StoryObj<typeof Footer>;

export const Text: Story = {
  
};
